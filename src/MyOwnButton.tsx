// @ts-nocheck
import * as React from 'react';
import { Button } from '@patternfly/react-core';

export const MyOwnButton = ({ children }) => <Button variant="warning">{children}</Button>;

export default MyOwnButton;
